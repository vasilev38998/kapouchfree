{
  "name": "vibe-social-network",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "db:print": "node -e \"import('node:fs').then(fs=>process.stdout.write(fs.readFileSync('database/schema.sql','utf8')))\""
  },
  "dependencies": {
    "mysql2": "^3.15.0"
  },
  "engines": {
    "node": ">=20"
  }
}
